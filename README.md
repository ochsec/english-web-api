# English Web API
A proof-of-concept example of a plain English client/server request/response using ChatGPT to translate text to a SQL query. The response is sent back to the client in .csv format and converted to a UI table by ChatGPT.

This example uses the database __, one of the example databases provided by Microsoft.

